# LoadBalancer: exposes the API to external traffic.
# This is the ONLY pod that receives traffic from outside the cluster.
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
